version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env  # <--- O Docker agora lê suas chaves aqui
    stdin_open: true
    tty: true